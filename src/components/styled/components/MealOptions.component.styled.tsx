import styled from 'styled-components';

const StyledMealOptions = styled.div`
    border: 1px solid black;
    padding: 5px;

    > .meal-options {
        display: flex;
    }
`;

export default StyledMealOptions;
