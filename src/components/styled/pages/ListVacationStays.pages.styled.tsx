import styled from 'styled-components';

const StyledListVacationStays = styled.div`
    display: flex;
    flex-wrap: wrap;
    justify-content: center;

    > .card-link {
        color: black;

        > a {
            text-decoration: none;
        }
    }
`;

export default StyledListVacationStays;
